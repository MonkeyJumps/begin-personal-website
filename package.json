{
  "name": "begin-app",
  "version": "0.0.0",
  "description": "Begin basic Hello World! app for Deno",
  "scripts": {
    "start": "run-s cross-env",
    "cross-env": "cross-env NODE_ENV=testing sandbox"
  },
  "devDependencies": {
    "cross-env": "^7.0.0"
  },
  "dependencies": {
    "@architect/sandbox": "^1.12.2",
    "npm-run-all": "^4.1.5"
  }
}
